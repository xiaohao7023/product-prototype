# 线下门店配置 — 需求终稿

> 基于 APP 版需求文档，适配网页版场景。标注 [网版调整] 为需要适配的部分。

---

## 模块一：店铺配置说明

### 店铺封面配置

| 配置项 | 说明 | 约束 |
|--------|------|------|
| 视频 | 上传 1 个视频作为店铺封面 | MP4 格式，≤60 秒，建议 16:9 比例 |
| 图片 | 上传 0-9 张图片 | JPG/PNG，单张 ≤2MB |
| 图片轮播 | 上传的图片在用户端以轮播方式展示 | 间隔 3 秒自动轮播，支持手动滑动 |

### 允许门店调整

- 开关：是否允许子门店对店铺封面进行个性化配置
- 关闭时：由企业总部统一配置，子门店不可编辑
- 开启时：子门店可独立调整封面内容

---

## 模块二：服务配置说明

> ⚠️ 此模块涉及核心业务逻辑，修改后将直接影响会员绑定关系和服务流程，请谨慎操作。

### 2.1 会员碰一碰绑定到店铺

| 项目 | 说明 |
|------|------|
| 描述 | 会员通过手机「碰一碰」或扫码访问配置页面后，自动绑定到当前店铺 |
| 默认状态 | 关闭，可手动开启 |

**绑定后的效果（仅展示）：**
- 查看会员打卡
- 查看会员饮食打卡
- 查看会员档案

[网版调整] APP 版描述为"手机碰一碰"，网页版改为"扫码或碰一碰"，覆盖更多入口场景。

### 2.2 系统引导会员进入 1V1 服务群

| 项目 | 说明 |
|------|------|
| 描述 | 开启后，绑定的会员系统将自动创建 1V1 服务群，您可以在群里触达会员 |
| 默认状态 | **开启**（可手动关闭） |

**服务群效果（仅展示）：**
- 商家在群里和会员沟通
- AI 助手提醒会员训练
- AI 助手解答会员问题

**入群机制说明：**
开启后将会自动将店长和会员拉入群聊，如需调度其他人，请在商家端 APP 操作。

[网版调整] APP 版支持在网页端选择入群成员，网页版简化为提示文案，成员调度统一在商家端 APP 完成。

### 2.3 允许门店调整

- 开关：是否允许子门店对服务配置进行个性化配置
- 关闭时：由企业总部统一配置
- 开启时：子门店可独立调整服务配置

---

## 模块三：功能配置说明

### 3.1 功能选择机制

点击「添加功能模块」按钮，弹出选择弹窗，包含两类：

**现有功能：**
- 记录训练 — 跳转到会员端训练页面
- 器械教学 — 跳转到会员端音视频页面
- 训练提醒 — 跳转到会员端训练提醒设置页面
- 饮食打卡 — 跳转到会员端饮食打卡页面
- 场馆服务 — 跳转到店铺商品购买页面
- 添加微信 — 需上传企微或个微二维码

**自定义配置：**
- H5 链接 — 自定义 H5 网页跳转
- 小程序链接 — 跳转到指定小程序页面

已添加的功能以卡片形式展示，每张卡片包含：序号、图标、标题、类型标签。

### 3.2 功能卡片操作

- **添加：** 点击弹窗中未使用的功能，添加后出现在列表最后
- **删除：** 点击卡片右上角删除按钮，移除该功能
- **排序：** 支持上移/下移调整展示顺序
- **编辑：** 点击卡片进入编辑模式

### 3.3 标题和描述编辑

允许商家对功能卡片进行标题和描述修改：

**标题规则：**
- 商家可自定义，限制 **10 个字**内
- 超出内容显示省略号 `...`

**描述规则：**
- 商家可自定义，限制 **15 个字**内
- 超出内容显示省略号 `...`

**功能说明（服务端返回，不可编辑）：**
- 功能描述：服务端下发
- 功能配图：服务端配置
- 去查看：跳转到对应的小程序页面

### 3.4 自定义「添加微信」录入

| 字段 | 规则 |
|------|------|
| 标题 | 商家可自定义，限制 10 个字内，超出显示省略号 |
| 描述 | 商家可自定义，限制 15 个字内，超出显示省略号 |
| 微信二维码 | 需上传 1 张图片，大小 ≤ 3MB，裁剪比例 1:1 |
| 查看示例图 | 示例图配置在服务端，可点击查看参考 |

### 3.5 允许门店调整

- 开关：是否允许子门店对功能配置进行个性化配置
- 关闭时：由企业总部统一配置
- 开启时：子门店可独立调整功能模块

---

## 页面布局

```
线下门店配置（全屏详情页）
├── 左侧：手机预览（实时更新封面视频/图片和功能模块）
└── 右侧配置区
    ├── 上方两列
    │   ├── 店铺封面配置（视频 + 图片，含允许门店调整开关）
    │   └── 会员服务配置（碰一碰绑定 + 1V1服务群，含允许门店调整开关）
    └── 下方全宽
        └── 功能配置（功能卡片列表 + 添加/删除/排序/编辑，含允许门店调整开关）
```

---

## 网页版新增/调整项汇总

| 序号 | 调整点 | 原因 |
|------|--------|------|
| 1 | 碰一碰描述补充"扫码"入口 | 网页版无 NFC，用户主要通过扫码进入 |
| 2 | 图片轮播在前端实现 | 网页版需自带轮播组件 |
| 3 | 入群成员调度统一在商家端 APP 完成 | 网页版不做成员选择，简化配置流程 |
| 4 | 各模块新增"允许门店调整"开关 | 网页版新增，子门店独立配置能力 |

---

*文档版本：v1.1 与原型同步*
*日期：2026-06-10*
